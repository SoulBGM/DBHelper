projectPath = E:\\IDEAFile\\DBHelper
# docsPath = 文档输出目录（非必须，默认是${projectPath}/apidocs）
# codeTplPath = 代码模版目录 (非必须，如果你需要自定义生成的代码才会用到。)
# mvcFramework = [spring, play, jfinal, generic](非必须，代码内部有判断，如果出现误判的情况，可以通过这个强制指定)